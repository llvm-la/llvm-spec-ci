# SPEC CPU 2017 Configuration: clang/flang on LoongArch
# Format for runcpu v6612 (SPEC 2017 v1.0.5)

strict_rundir_verify = 0

default= base:
   CC = @@BUILD_DIR@@/bin/clang
   CXX = @@BUILD_DIR@@/bin/clang++
   FC = @@BUILD_DIR@@/bin/flang
   CFLAGS = -O2 -march=loongarch64
   CXXFLAGS = -O2 -march=loongarch64 -std=c++14
   FFLAGS = -O2 -march=loongarch64
   LD = @@BUILD_DIR@@/bin/clang
   LDFLAGS = -Wl,--strip-all
   output_format = html
